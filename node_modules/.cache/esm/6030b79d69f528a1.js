let showMessage;_69f‍.w("../controllers/auth",[["showMessage",["showMessage"],function(v){showMessage=v}]]);const express = require("express");


const router = express.Router();

// Routes
router.get('/:message', showMessage)

module.exports = router;