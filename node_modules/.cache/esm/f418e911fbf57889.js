let admin;_fbf‍.x([["currentUser",()=>currentUser]]);_fbf‍.w("../firebase",[["default",["admin"],function(v){admin=v}]]);

       const currentUser = async (req, res) => {
    // console.log("REQ HEADERS TOKEN", req.headers.token)
    try {
        //verify token
        const firebaseUser = await admin.auth().verifyIdToken(req.headers.token);
        _fbf‍.g.console.log("FIREBASE USER IN CURRENT USER MIDDLEWARE", firebaseUser)
        res.json(firebaseUser)
    }
    catch(err) {
      _fbf‍.g.console.log(err)
      res.status(401).json({
          err: "Invalid or expired token"
      })
    }
}